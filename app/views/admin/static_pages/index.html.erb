<div class="container-fluid">
  <%= render "shared/search" %>
  <div class="container pt-5 pb-3">
    <div class="row">
      <% if @tours.empty? %>
        <%= t ".no_tours_msg" %>
      <% else %>
        <%= render @tours %>
      <% end %>
    </div>
  </div>
  <%== pagy_bootstrap_nav @pagy %>
</div>
